name: underwater_fish_detection
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - numpy
  - pandas
  - matplotlib
  - seaborn
  - scikit-learn
  - jupyter
  - opencv
  - pytorch
  - torchvision
  - torchaudio
  - pip
  - pip:
      - dvc
      - albumentations
      - tqdm
      - kaggle
      - ultralytics    # optional if you use YOLO models
